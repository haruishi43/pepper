sampler = dict(
    type="InfiniteNaiveIdentityDistributedSampler",
    batch_size=128,
    num_instances=4,
    shuffle=True,
)
# sampler = dict(
#     type="NaiveIdentityDistributedSampler",
#     batch_size=128,
#     num_instances=4,
#     shuffle=True,
# )
